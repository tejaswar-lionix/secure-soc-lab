version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: sentinel
      POSTGRES_USER: sentinel
      POSTGRES_PASSWORD: sentinel
    ports: ["5432:5432"]
  web:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
